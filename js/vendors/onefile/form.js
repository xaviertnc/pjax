// F1.Form.prototype.postData = function (options, data)
// {
  // options = options || {};
  // options.method = 'post';
  // options.data = data || {};
  // options.dataType = options.dataType || 'html';
  // this.eventsService.trigger('form.beforePostData');
  // return $.ajax(options)
    // .done(this.onPostSuccess.bind(this))
    // .fail(this.onPostFail.bind(this));  
// };
